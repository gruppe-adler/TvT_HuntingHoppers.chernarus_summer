#ifndef MODULES_DIRECTORY
    #define MODULES_DIRECTORY modules
#endif

class grad_dynGroupDecals {
    class all {
        file = MODULES_DIRECTORY\grad-dynGroupDecals\functions;

        class init {postInit = 1; };
        class refreshDecals;
        class setTexture;
    };
};